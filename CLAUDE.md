# 뭐하지? (mwohaji)

## 이 앱의 목적

**선택장애가 있는 사람이 "지금 이 조건에서 뭘 할지"를 빠르게 결정하도록 돕는 앱.**

이게 가장 중요한 기준이다. 기능을 추가하거나 바꿀 때 항상 물어볼 것:
"이 변경이 사용자의 결정을 더 쉽게 만드는가, 아니면 고민거리를 하나 더 늘리는가?"

- 선택지는 **3~4개로 제한**한다. 많을수록 결정이 어려워진다.
- 1순위 항목에는 "✨ 제일 추천" 배지와 "🎯 고민 그만, 이걸로 결정!" 버튼이 붙는다.
- 결과는 항상 **적합한 순서로 정렬**된다. 1번이 곧 최선의 답이어야 한다.

## 구조

단일 HTML 파일(`index.html`)이다. 빌드 과정이 없고 브라우저에서 바로 열린다.
CSS, JS 전부 이 파일 안에 있다. 파일이 크므로 **전체 재작성 대신 부분 수정**을 할 것.

```
index.html          앱 전체 (HTML + CSS + JS)
worker/              무료 AI 프록시(Cloudflare Workers, 선택 배포) — worker/README.md 참고
CLAUDE.md           이 문서
README.md           사용자용 설명
.nojekyll           GitHub Pages에서 필요
```

### 주요 섹션 (index.html 안에서 검색용 앵커)

| 앵커 | 내용 |
|---|---|
| `:root{` | 색상 토큰 (파스텔 팔레트) |
| `MBTI_PROFILES` | 16유형별 성향 라벨 + 키워드 |
| `MBTI_AXIS_PREF` | E/I·N/S·F/T·P/J 축별 선호 키워드 |
| `MOOD_SYNONYMS` | 기분 표현 → 내부 키워드 확장 |
| `EXCLUDE_CATEGORIES` | 제외 가능 범주 10종 + 분류 규칙 |
| `OFFLINE_BANK` | 오프라인 추천 활동 196개 |
| `STATUS_POOL` | 상태 문구 랜덤 풀 |
| `fallbackRecommendations` | **추천 엔진 핵심** |
| `buildReason` | 추천 이유 문장 생성 |
| `fetchRecommendations` | AI 추천 (Gemini / Anthropic) |
| `paintTimetable` | 시간표 렌더링 |
| `shareTimetableImage` | 시간표 이미지 저장/공유 |

## 추천 엔진 규칙 (절대 깨지 말 것)

`fallbackRecommendations` 안의 필터/점수 로직이다.

### 하드 필터 — 절대 완화 금지
1. **미성년자 안전**: 나이 19세 미만이면 `adultOnly` 항목은 어떤 경우에도 제외.
2. **사용자 제외 범주**: 사용자가 직접 뺀 범주는 후보가 부족해져도 되돌리지 않는다.
   결과가 3개 미만이면 억지로 채우지 말고 안내 문구(`note`)를 띄운다.

### 예산 규칙
- `무료` 선택 → `minMoney === 0`인 항목만.
- 그 외 선택 → **무료는 제외**하고 `1 <= minMoney <= 선택값`인 항목만.
  (~3만원을 골랐으면 실제로 1~3만원을 쓰는 활동이 나와야 한다. 산책 같은 무료 활동이 섞이면 안 됨.)

### 시간 규칙
- `budgetTime - 1 <= minTime <= budgetTime` 밴드로 거른다.
  2시간을 골랐는데 30분짜리가 나오면 안 된다.

### 점수 가중치 — 상한(cap)이 핵심
사용자가 **직접 입력한 기분**이 항상 가장 강한 신호여야 한다.
과거에 나이·MBTI 점수가 기분을 눌러서 "힐링"을 입력했는데 "축제 구경"이 1순위로
나오는 버그가 있었다. 상한을 제거하지 말 것.

| 신호 | 개당 | 상한 |
|---|---|---|
| 기분(mood) | 4 | 8 |
| MBTI 키워드 | 2 | 4 |
| MBTI 축 | 1 | 2 |
| 나이 | 1 | 2 |
| 시간 적합도 | `3 - |차이|*2` | — |
| 장소 적합도 | 외출형 +2 / 집에서 -3 (구체적 장소 입력 시에만) | — |
| 최근 노출(novelty) | 최근 보여준 항목이면 -3 | — |

novelty는 `recentlyShown`(세션 동안만 유지, 최근 12개)에 있는 title을 감점하는 것뿐이다.
"다시 뽑기"를 눌러도 매번 똑같은 1순위가 나오지 않게 하려는 용도이며, mood 상한(8)보다
작게 유지해서 "기분이 항상 최강 신호"라는 원칙은 깨지 않는다.

### 다양성
점수 상위만 뽑으면 비슷한 활동이 몰린다. `mood[0]` + `categoryOf(it)`(EXCLUDE_CATEGORIES 기준
1차 범주) 조합이 겹치지 않는 항목을 먼저 고르고, 부족하면 점수순으로 채운다. `mood[0]`만 보면
"카페"와 "카페"처럼 대표 키워드는 같은데 사실상 같은 종류인 활동이 같이 나올 수 있어서 범주도 함께 본다.

## 데이터 스키마

`OFFLINE_BANK` 항목:

```js
{
  icon:'☕',                    // 이모지 1개
  title:'동네 카페서 커피 한 잔',  // 6~14자
  desc:'짧게 앉았다 갈 만한 곳',   // 12~24자
  minTime:1,                    // 1=30분 2=1시간 3=2시간 4=반나절+
  minMoney:1,                   // 0=무료 1=~1만원 2=~3만원 3=5만원+
  solo:'any',                   // 'any' | '혼자' | '같이'
  mood:['조용히','힐링','편하게'], // 매칭 키워드 (첫 번째가 대표 성격)
  dayOnly:true,                 // 선택: 밤/새벽에 제외
  adultOnly:true                // 선택: 미성년자에게 제외
}
```

시간×예산 조합 중 얕은 칸(예: 반나절+무료, 30분+비쌈)이 있으면 다양성이 떨어지니, 새 항목을
추가할 땐 `node -e`로 칸별 개수를 세어서 얕은 칸부터 채울 것 (한 방향으로만 몰아넣지 말기).

**mood 키워드는 정해진 어휘를 쓴다** (새로 만들지 말 것):
`힐링 조용히 편하게 피곤 집중 몰입 신나게 즉흥 모험 상쾌 스트레스 아무거나`

`title`/`desc`에 "동네", "근처"를 쓰면 사용자가 입력한 지역명으로 자동 치환된다.

## 사용자 프로필 (나이·성별·MBTI)

- "뭐하지?" 버튼(`btnStart`)을 처음 누르면 계정 로그인이 아니라 **로컬 프로필 온보딩 모달**(`mbtiModal`)이 한 번 뜬다.
  나이·성별·MBTI를 입력(또는 스킵)하면 `profile_onboarded`가 저장되고 이후로는 바로 폼으로 진행한다.
- 나이·MBTI는 `fallbackRecommendations` 점수(가중치표 참고)에 실제로 반영된다.
- 성별(`userGender`)은 저장·AI 프롬프트(`fetchRecommendations`)에는 넘기지만, 오프라인 폴백 점수에는 반영하지 않는다.
  168개 활동을 성별로 나눠 태깅하는 것 자체가 고정관념이 되기 쉬워서, 성별 반영은 문맥 판단이 가능한 AI 경로에만 맡기고
  AI 시스템 프롬프트에 "고정관념적 취향 매칭 금지, 안전 등 실질적 관련 있을 때만 참고"라고 명시해뒀다. 이 원칙은 유지할 것.

## AI 추천

- Claude 아티팩트 안: `window.storage`가 있으면 키 없이 내장 Anthropic 호출.
- 외부 호스팅: `AI_PROXY_URL`(index.html 상단 근처)에 배포된 무료 프록시 주소가 있으면 방문자는 키 입력 없이 바로 AI 추천을 받는다.
  프록시는 `worker/`에 있는 Cloudflare Workers 코드로, 배포자의 무료 Gemini 키를 서버 측에서만 사용하고 IP당 하루 요청 수를 제한한다.
  배포 방법은 `worker/README.md` 참고. `AI_PROXY_URL`이 비어 있으면(기본값) 이 경로는 건너뛴다.
- 프록시가 없거나 방문자가 원하면, 기존처럼 **본인 키**를 직접 넣을 수도 있다(Gemini 무료 또는 Anthropic 유료). 본인 키가 있으면 프록시보다 그 키가 우선한다.
- 키는 `localStorage`에만 저장하고 서버로 보내지 않는다. 프록시의 `GEMINI_API_KEY`는 Cloudflare Secret으로만 보관되고 브라우저로 내려오지 않는다.
- AI 실패/키 없음 → 조용히 `fallbackRecommendations`로 폴백. **앱은 항상 동작해야 한다.**

## 시간표(추천 항목 → 30분 슬롯)

`buildSlots`가 선택한 시간(`SLOT_COUNT_MAP`: 30분→1칸 ~ 반나절→8칸)만큼 지금부터 30분 단위
슬롯을 만든다. 추천 항목마다 `slots`(1~4, `OFFLINE_BANK`의 `minTime`과 같은 단위) 필드가 붙는데,
오프라인 폴백은 `it.minTime`을 그대로 쓰고 AI 경로는 응답 JSON에 `slots`를 요청하고 없으면 1로 보정한다.

`#timetableAuto`("✨ 자동 채우기")는 빈 슬롯을 30분씩 기계적으로 채우지 않는다.
빈 구간의 길이를 먼저 재고, 그 길이에 가장 잘 맞는 추천 항목(`slots` 기준)을 골라 그 항목의
실제 길이만큼 연속 슬롯에 이어붙인다. 후보가 여럿이면 바로 직전에 배치한 항목은 피한다.
반나절 시간표를 자동으로 채워도 "카페 30분 → 영화 2시간 → 산책 30분"처럼 하루 일정처럼
보이게 하려는 의도이니, 이 로직을 다시 라운드로빈으로 되돌리지 말 것.

## 저장소 어댑터

`store` 객체가 `window.storage`(아티팩트)와 `localStorage`(외부)를 모두 처리한다.
`window.storage`를 직접 호출하지 말고 반드시 `store`를 쓸 것.

## 스타일

- 밝고 귀여운 파스텔 톤. 색은 `:root` 토큰만 쓴다.
- 폰트: `Jua`(제목·버튼), `Gowun Dodum`(본문), `Gaegu`(티켓 손글씨).
- 하드코딩된 hex 색을 새로 추가하지 말고 토큰을 쓸 것.

## 테스트

빌드가 없으므로 로직 변경 후에는 노드로 함수를 뽑아 시뮬레이션한다:

```bash
node test-engine.js
```

추천 로직을 건드렸다면 반드시 확인할 것:
- 무료 선택 시 유료 항목이 안 섞이는지
- 미성년자 조건에서 `adultOnly` 노출이 0인지
- 제외 범주가 지켜지는지
- 기분 입력이 결과에 제대로 반영되는지
