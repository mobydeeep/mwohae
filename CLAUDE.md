# 뭐하지? (mwohaji)

## 이 앱의 목적

**선택장애가 있는 사람이 "지금 이 조건에서 뭘 할지"를 빠르게 결정하도록 돕는 앱.**

이게 가장 중요한 기준이다. 기능을 추가하거나 바꿀 때 항상 물어볼 것:
"이 변경이 사용자의 결정을 더 쉽게 만드는가, 아니면 고민거리를 하나 더 늘리는가?"

- 선택지는 **3~4개로 제한**한다. 많을수록 결정이 어려워진다.
- 1순위 항목에는 "✨ 제일 추천" 배지와 "🎯 고민 그만, 이걸로 결정!" 버튼이 붙는다.
- 결과는 항상 **적합한 순서로 정렬**된다. 1번이 곧 최선의 답이어야 한다.

## 구조

단일 HTML 파일(`index.html`)이다. 빌드 과정이 없고 브라우저에서 바로 열린다.
CSS, JS 전부 이 파일 안에 있다. 파일이 크므로 **전체 재작성 대신 부분 수정**을 할 것.

```
index.html          앱 전체 (HTML + CSS + JS)
CLAUDE.md           이 문서
README.md           사용자용 설명
.nojekyll           GitHub Pages에서 필요
```

### 주요 섹션 (index.html 안에서 검색용 앵커)

| 앵커 | 내용 |
|---|---|
| `:root{` | 색상 토큰 (파스텔 팔레트) |
| `MBTI_PROFILES` | 16유형별 성향 라벨 + 키워드 |
| `MBTI_AXIS_PREF` | E/I·N/S·F/T·P/J 축별 선호 키워드 |
| `MOOD_SYNONYMS` | 기분 표현 → 내부 키워드 확장 |
| `EXCLUDE_CATEGORIES` | 제외 가능 범주 10종 + 분류 규칙 |
| `OFFLINE_BANK` | 오프라인 추천 활동 168개 |
| `STATUS_POOL` | 상태 문구 랜덤 풀 |
| `fallbackRecommendations` | **추천 엔진 핵심** |
| `buildReason` | 추천 이유 문장 생성 |
| `fetchRecommendations` | AI 추천 (Gemini / Anthropic) |
| `paintTimetable` | 시간표 렌더링 |
| `shareTimetableImage` | 시간표 이미지 저장/공유 |

## 추천 엔진 규칙 (절대 깨지 말 것)

`fallbackRecommendations` 안의 필터/점수 로직이다.

### 하드 필터 — 절대 완화 금지
1. **미성년자 안전**: 나이 19세 미만이면 `adultOnly` 항목은 어떤 경우에도 제외.
2. **사용자 제외 범주**: 사용자가 직접 뺀 범주는 후보가 부족해져도 되돌리지 않는다.
   결과가 3개 미만이면 억지로 채우지 말고 안내 문구(`note`)를 띄운다.

### 예산 규칙
- `무료` 선택 → `minMoney === 0`인 항목만.
- 그 외 선택 → **무료는 제외**하고 `1 <= minMoney <= 선택값`인 항목만.
  (~3만원을 골랐으면 실제로 1~3만원을 쓰는 활동이 나와야 한다. 산책 같은 무료 활동이 섞이면 안 됨.)

### 시간 규칙
- `budgetTime - 1 <= minTime <= budgetTime` 밴드로 거른다.
  2시간을 골랐는데 30분짜리가 나오면 안 된다.

### 점수 가중치 — 상한(cap)이 핵심
사용자가 **직접 입력한 기분**이 항상 가장 강한 신호여야 한다.
과거에 나이·MBTI 점수가 기분을 눌러서 "힐링"을 입력했는데 "축제 구경"이 1순위로
나오는 버그가 있었다. 상한을 제거하지 말 것.

| 신호 | 개당 | 상한 |
|---|---|---|
| 기분(mood) | 4 | 8 |
| MBTI 키워드 | 2 | 4 |
| MBTI 축 | 1 | 2 |
| 나이 | 1 | 2 |
| 시간 적합도 | `3 - |차이|*2` | — |
| 장소 적합도 | 외출형 +2 / 집에서 -3 (구체적 장소 입력 시에만) | — |

### 다양성
점수 상위만 뽑으면 비슷한 활동이 몰린다. `mood[0]`이 겹치지 않도록 먼저 고르고,
부족하면 점수순으로 채운다.

## 데이터 스키마

`OFFLINE_BANK` 항목:

```js
{
  icon:'☕',                    // 이모지 1개
  title:'동네 카페서 커피 한 잔',  // 6~14자
  desc:'짧게 앉았다 갈 만한 곳',   // 12~24자
  minTime:1,                    // 1=30분 2=1시간 3=2시간 4=반나절+
  minMoney:1,                   // 0=무료 1=~1만원 2=~3만원 3=5만원+
  solo:'any',                   // 'any' | '혼자' | '같이'
  mood:['조용히','힐링','편하게'], // 매칭 키워드 (첫 번째가 대표 성격)
  dayOnly:true,                 // 선택: 밤/새벽에 제외
  adultOnly:true                // 선택: 미성년자에게 제외
}
```

**mood 키워드는 정해진 어휘를 쓴다** (새로 만들지 말 것):
`힐링 조용히 편하게 피곤 집중 몰입 신나게 즉흥 모험 상쾌 스트레스 아무거나`

`title`/`desc`에 "동네", "근처"를 쓰면 사용자가 입력한 지역명으로 자동 치환된다.

## AI 추천

- Claude 아티팩트 안: `window.storage`가 있으면 키 없이 내장 Anthropic 호출.
- 외부 호스팅: 방문자가 **본인 키**를 넣는다. Gemini(무료) 또는 Anthropic(유료).
- 키는 `localStorage`에만 저장하고 서버로 보내지 않는다.
- AI 실패/키 없음 → 조용히 `fallbackRecommendations`로 폴백. **앱은 항상 동작해야 한다.**

## 저장소 어댑터

`store` 객체가 `window.storage`(아티팩트)와 `localStorage`(외부)를 모두 처리한다.
`window.storage`를 직접 호출하지 말고 반드시 `store`를 쓸 것.

## 스타일

- 밝고 귀여운 파스텔 톤. 색은 `:root` 토큰만 쓴다.
- 폰트: `Jua`(제목·버튼), `Gowun Dodum`(본문), `Gaegu`(티켓 손글씨).
- 하드코딩된 hex 색을 새로 추가하지 말고 토큰을 쓸 것.

## 테스트

빌드가 없으므로 로직 변경 후에는 노드로 함수를 뽑아 시뮬레이션한다:

```bash
node test-engine.js
```

추천 로직을 건드렸다면 반드시 확인할 것:
- 무료 선택 시 유료 항목이 안 섞이는지
- 미성년자 조건에서 `adultOnly` 노출이 0인지
- 제외 범주가 지켜지는지
- 기분 입력이 결과에 제대로 반영되는지
